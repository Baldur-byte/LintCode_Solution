#include <iostream>
#include <vector>

using namespace std;

class Solution
{
public:
    /**
     * @param boxes: number of pens for each box
     * @param target: the target number
     * @return: the minimum boxes
     */
    int minimumBoxes(vector<int>& boxes, int target)
    {
        // write your code here
    }
};

int main()
{
    Solution s = Solution();
    vector<int> boxs;
    int target = 0;
    s.minimumBoxes(boxs, target);
}